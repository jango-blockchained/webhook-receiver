{
  "name": "webhook-receiver",
  "version": "1.0.0",
  "description": "Public webhook endpoint for TradingView signals",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage"
  },
  "dependencies": {
    "itty-router": "^5.0.18"
  },
  "devDependencies": {
    "wrangler": "^4.4.0",
    "bun-types": "latest",
    "@types/jest": "^29.5.12",
    "jest-mock": "^29.7.0"
  }
}
